# Copy this file to .env.local and fill in your key to enable the live AI assistant.
# Without it, the assistant automatically falls back to a small built-in
# bilingual response set (see src/lib/assistant/fallback.ts), so the site
# still works fully offline for demos.
ANTHROPIC_API_KEY=
